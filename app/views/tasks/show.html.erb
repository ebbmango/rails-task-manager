<h1><%= @task.title %></h1>
<p><%= @task.completed %></p>
<p><%= @task.details %></p>


<%= link_to 'Back to the task list', tasks_path %>
<%= link_to 'Edit task', "#{@task[:id]}/edit" %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
